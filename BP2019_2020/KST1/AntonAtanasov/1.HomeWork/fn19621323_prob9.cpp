#include <iostream>
#include <math.h>
using namespace std;
int main()
{
	unsigned long a, h, S;
	cout << "Vyvedete a = ";
	cin >> a;
	cout << "Vyvedete h = ";
	cin >> h;
	S = (a * h) / 2;
	cout << "S = " << S;
	return 0;
}