#include <iostream>
using namespace std;

int main()
{
	int A, B;
	cin >> A >> B;
	float C = A / B; 
	cout << C << endl;
	float D = float(A) / B;
	cout << D << endl;

	//При С когато резултата от делението не е цяло число ще ни показва само 
	// цялата част, защото А и B са от тип int

	//При D когато резултата от делението не е цяло число ще ни показва цялата
	// стойност, защото преобразуваме променливата А във променлива с плаваща запетая.
}