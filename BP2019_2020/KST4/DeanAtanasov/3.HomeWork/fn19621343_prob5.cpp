#include <iostream>
#include <math.h>
using namespace std;
int main ()
{
  double x;
  int y = -4;

  if ( y < 1) {

   cout << " x = " << pow ( y , 3) + ( pow (y , 4) + (2 * y) );
  }
  else if ( y > 1) {

   cout << " x = " << 2 * ((2 * y) + 5) / (14 - (y / 3)) ;
  }

   return 0;






}

